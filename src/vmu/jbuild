(jbuild_version 1)

(library
 ((name Vmu)
  (libraries (bitstring str core Compiler sedlex))
  (preprocess (pps (ppx_bitstring sedlex.ppx ppx_deriving.show ppx_deriving.eq ppx_deriving.ord)))
  (wrapped true)))

